# Limitations

No mainnet token deployment is activated. The included Solidity contract is a deployable reference and should be audited before real value. Vercel demo uses SQLite in `/tmp`; production should use Postgres and an audited factory/DEX router.
